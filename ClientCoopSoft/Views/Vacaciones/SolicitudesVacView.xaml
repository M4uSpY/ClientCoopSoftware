<UserControl
    x:Class="ClientCoopSoft.Views.Vacaciones.SolicitudesVacView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ClientCoopSoft.Views.Vacaciones"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>

        <StackPanel
            Grid.Row="0"
            Grid.Column="0"
            Orientation="Horizontal">
            <Button
                Width="35"
                Height="35"
                Margin="0,0,0,10"
                Background="#1E0F74"
                BorderBrush="Transparent"
                BorderThickness="0"
                Command="{Binding VolverCommand}"
                Foreground="#333"
                Style="{StaticResource MaterialDesignFloatingActionMiniLightButton}">
                <md:PackIcon
                    Width="20"
                    Height="30"
                    Foreground="White"
                    Kind="ArrowLeft" />
            </Button>
            <TextBlock
                Margin="10,0,0,20"
                FontSize="24"
                FontWeight="SemiBold"
                Text="Lista de Solicitudes" />
        </StackPanel>
        <Border
            Grid.Row="1"
            Grid.ColumnSpan="3"
            Margin="0,0,0,10"
            Padding="8"
            Background="#F5F5F7"
            CornerRadius="8">
            <DockPanel>
                <md:PackIcon
                    Width="20"
                    Height="20"
                    Margin="0,0,8,0"
                    VerticalAlignment="Center"
                    Foreground="#757575"
                    Kind="Magnify" />
                <TextBox
                    Width="320"
                    md:HintAssist.Hint="Buscar por CI, nombre, cargo, tipo, motivo, fecha, estado..."
                    Background="Transparent"
                    BorderThickness="0"
                    Text="{Binding TextoBusqueda, UpdateSourceTrigger=PropertyChanged}" />
            </DockPanel>
        </Border>
        <DataGrid
            Grid.Row="2"
            Grid.ColumnSpan="3"
            Margin="0"
            AutoGenerateColumns="False"
            Background="White"
            BorderThickness="1"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            IsReadOnly="True"
            ItemsSource="{Binding SolicitudesView}">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="auto"
                    Binding="{Binding IdVacacion}"
                    Header="No" />
                <DataGridTextColumn
                    Width="Auto"
                    Binding="{Binding CI}"
                    Header="CI" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding ApellidosNombres}"
                    Header="Apellidos y Nombres" />
                <DataGridTextColumn
                    Width="Auto"
                    Binding="{Binding Cargo}"
                    Header="Cargo" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Tipo}"
                    Header="Tipo" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Motivo}"
                    Header="Motivo" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding FechaInicio}"
                    Header="Fecha Inicio" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding FechaFin}"
                    Header="Fecha Fin" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Estado}"
                    Header="Estado" />
                <DataGridTemplateColumn Width="250" Header="Acciones">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">

                                <!--  EDITAR  -->
                                <Button
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    Command="{Binding DataContext.EditarSolicitudCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}">
                                    <md:PackIcon
                                        Width="16"
                                        Height="16"
                                        Foreground="#1976D2"
                                        Kind="Pencil" />
                                </Button>
                                <!--  AprobAR  -->
                                <Button
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    BorderThickness="0"
                                    Command="{Binding DataContext.AprobarSolicitudCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}">
                                    <md:PackIcon
                                        Width="16"
                                        Height="16"
                                        Foreground="#22B14C"
                                        Kind="Check" />
                                </Button>

                                <!--  RECHAZAR  -->
                                <Button
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    BorderThickness="0"
                                    Command="{Binding DataContext.RechazarSolicitudCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}">
                                    <md:PackIcon
                                        Width="16"
                                        Height="16"
                                        Foreground="#D9534F"
                                        Kind="Delete" />
                                </Button>



                                <!--  ELIMINAR  -->
                                <Button
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    BorderThickness="0"
                                    Command="{Binding DataContext.EliminarSolicitudCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}">
                                    <md:PackIcon
                                        Width="16"
                                        Height="16"
                                        Foreground="#B71C1C"
                                        Kind="TrashCan" />
                                </Button>


                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>



            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
