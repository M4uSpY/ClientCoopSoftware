<UserControl
    x:Class="ClientCoopSoft.Views.Planillas.PlanillaSueldosSalariosView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:ClientCoopSoft.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converters:BoolToTextoCerradaConverter x:Key="BoolToTextoCerradaConverter" />
        <converters:BoolToBrushConverter x:Key="BoolToBrushConverter" />
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  TÍTULO  -->
        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,10"
            FontSize="24"
            FontWeight="SemiBold"
            Foreground="#303048"
            Text="Planilla de Sueldos y Salarios" />

        <!--  PANEL DE AYUDA / PASOS  -->
        <Border
            Grid.Row="1"
            Margin="0,0,0,10"
            Padding="10"
            Background="#F5F7FB"
            BorderBrush="#D0D5E2"
            BorderThickness="1"
            CornerRadius="4">
            <StackPanel>
                <TextBlock
                    FontSize="12"
                    FontWeight="SemiBold"
                    Foreground="#303048"
                    Text="¿Cómo usar esta pantalla?" />
                <TextBlock
                    Margin="0,4,0,0"
                    FontSize="11"
                    Foreground="#555"
                    TextWrapping="Wrap">
                    1. Ingrese la Gestión y el Mes que desea procesar (ejemplo: 2025 / 11).
                </TextBlock>
                <TextBlock
                    FontSize="11"
                    Foreground="#555"
                    TextWrapping="Wrap">
                    2. Para una planilla nueva, haga clic en<Bold>“Crear y calcular”</Bold>
                    .
                    El sistema generará las filas de todos los trabajadores activos y calculará los montos.</TextBlock>
                <TextBlock
                    FontSize="11"
                    Foreground="#555"
                    TextWrapping="Wrap">
                    3. Para ver una planilla existente, escriba el<Bold>Id Planilla</Bold>
                    y presione<Bold>“Cargar”</Bold>
                    .</TextBlock>
                <TextBlock
                    FontSize="11"
                    Foreground="#555"
                    TextWrapping="Wrap">
                    4. Use<Bold>“Recalcular”</Bold>
                    si cambió conceptos manuales en otra pantalla.</TextBlock>
                <TextBlock
                    FontSize="11"
                    Foreground="#555"
                    TextWrapping="Wrap">
                    5. Cuando la planilla esté revisada, presione<Bold>“Cerrar”</Bold>
                    para bloquear más cambios.</TextBlock>
            </StackPanel>
        </Border>
        <!--  FILTROS + BOTONES  -->
        <!--  FILTROS + BOTONES  -->
        <DockPanel
            Grid.Row="2"
            Margin="0,0,0,10"
            LastChildFill="True">

            <!--  FILTROS: AÑO + MES + INFO PLANILLA  -->
            <StackPanel
                VerticalAlignment="Center"
                DockPanel.Dock="Left"
                Orientation="Horizontal">

                <TextBlock
                    Margin="0,0,8,0"
                    VerticalAlignment="Center"
                    FontSize="12"
                    Text="Filtrar planilla por:" />

                <!--  Año / Gestión  -->
                <StackPanel Margin="0,0,16,0" Orientation="Horizontal">
                    <ComboBox
                        Width="90"
                        Height="Auto"
                        Margin="0,5,0,0"
                        md:HintAssist.Hint="Año"
                        md:HintAssist.HintPaddingBrush="White"
                        md:TextFieldAssist.OutlinedBorderActiveThickness="1.5"
                        BorderBrush="#CCC"
                        IsEditable="False"
                        ItemsSource="{Binding Anios}"
                        SelectedItem="{Binding AnioSeleccionado, Mode=TwoWay}"
                        Style="{StaticResource MaterialDesignOutlinedComboBox}" />
                </StackPanel>

                <!--  Mes  -->
                <StackPanel Margin="0,0,16,0" Orientation="Horizontal">
                    <ComboBox
                        Width="130"
                        Height="Auto"
                        Margin="0,5,0,0"
                        md:HintAssist.Hint="Mes"
                        md:HintAssist.HintPaddingBrush="White"
                        md:TextFieldAssist.OutlinedBorderActiveThickness="1.5"
                        BorderBrush="#CCC"
                        DisplayMemberPath="Nombre"
                        IsEditable="False"
                        ItemsSource="{Binding Meses}"
                        SelectedItem="{Binding MesSeleccionado, Mode=TwoWay}"
                        Style="{StaticResource MaterialDesignOutlinedComboBox}" />
                </StackPanel>

                <!--  Id Planilla (solo lectura, opcional)

                                    <StackPanel Margin="0,0,16,0">
                                <TextBlock
                                    FontSize="11"
                                    Foreground="#666"
                                    Text="Id Planilla" />
                                <TextBlock
                                    Margin="0,5,0,0"
                                    FontSize="13"
                                    FontWeight="SemiBold"
                                    Foreground="#303048"
                                    Text="{Binding IdPlanillaActual}" />
                            </StackPanel>
                -->


                <!--  Estado cerrada / abierta  -->
                <StackPanel Margin="0,0,16,0" VerticalAlignment="Center">
                    <TextBlock
                        FontSize="11"
                        Foreground="#666"
                        Text="Estado" />
                    <TextBlock
                        Margin="0,2,0,0"
                        VerticalAlignment="Center"
                        FontWeight="SemiBold"
                        Foreground="{Binding EstaCerrada, Converter={StaticResource BoolToBrushConverter}}">
                        <Run Text="{Binding EstaCerrada, Converter={StaticResource BoolToTextoCerradaConverter}}" />
                    </TextBlock>
                </StackPanel>
            </StackPanel>

            <!--  BOTONES DE ACCIÓN  -->
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <!--  Crear + calcular  -->
                <Button
                    Margin="0,0,0,0"
                    Padding="12,4"
                    Background="#0078D7"
                    BorderThickness="0"
                    Command="{Binding CrearYCalcularPlanillaCommand}"
                    Content="Crear y calcular"
                    FontSize="9"
                    Foreground="White"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    ToolTip="Crear una nueva planilla para el año y mes indicados" />

                <!--  Cargar  -->
                <Button
                    Margin="8,0,0,0"
                    Padding="12,4"
                    Command="{Binding CargarPlanillaCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    ToolTip="Cargar la planilla para el año y mes seleccionados">
                    <StackPanel Orientation="Horizontal">
                        <md:PackIcon Margin="0,0,6,0" Kind="Refresh" />
                        <TextBlock Text="Cargar" />
                    </StackPanel>
                </Button>

                <!--  Recalcular  -->
                <Button
                    Margin="8,0,0,0"
                    Padding="12,4"
                    Command="{Binding RecalcularPlanillaCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}">
                    <StackPanel Orientation="Horizontal">
                        <md:PackIcon Margin="0,0,6,0" Kind="CalculatorVariant" />
                        <TextBlock Text="Recalcular" />
                    </StackPanel>
                </Button>

                <!--  Guardar RC-IVA  -->
                <Button
                    Margin="8,0,0,0"
                    Padding="12,4"
                    Command="{Binding GuardarRcIvaCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    ToolTip="Guardar valores manuales de RC-IVA e inmediatamente recalcular la planilla">
                    <StackPanel Orientation="Horizontal">
                        <md:PackIcon Margin="0,0,6,0" Kind="ContentSave" />
                        <TextBlock Text="Guardar RC-IVA" />
                    </StackPanel>
                </Button>
                <!--  Guardar Otros Desc.  -->
                <Button
                    Margin="8,0,0,0"
                    Padding="12,4"
                    Command="{Binding GuardarOtrosDescuentosCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    ToolTip="Guardar valores manuales de Otros Descuentos e inmediatamente recalcular la planilla">
                    <StackPanel Orientation="Horizontal">
                        <md:PackIcon Margin="0,0,6,0" Kind="ContentSaveEdit" />
                        <TextBlock Text="Guardar Otros Desc." />
                    </StackPanel>
                </Button>


                <!--  Cerrar planilla  -->
                <Button
                    Margin="8,0,0,0"
                    Padding="12,4"
                    Command="{Binding CerrarPlanillaCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}">
                    <StackPanel Orientation="Horizontal">
                        <md:PackIcon Margin="0,0,6,0" Kind="Lock" />
                        <TextBlock Text="Cerrar" />
                    </StackPanel>
                </Button>

                <!--  Exportar Excel  -->
                <Button
                    Width="40"
                    Height="40"
                    Margin="16,0,0,0"
                    Background="#1D9942"
                    BorderBrush="Transparent"
                    Command="{Binding ExportarExcelCommand}"
                    Style="{StaticResource MaterialDesignFloatingActionMiniLightButton}"
                    ToolTip="Exportar a Excel (.xlsx)">
                    <md:PackIcon Foreground="White" Kind="FileExcel" />
                </Button>


            </StackPanel>
        </DockPanel>


        <!--  DATA GRID  -->
        <DataGrid
            Grid.Row="3"
            Margin="0"
            AutoGenerateColumns="False"
            Background="White"
            BorderThickness="1"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            HeadersVisibility="Column"
            IsReadOnly="False"
            ItemsSource="{Binding PlanillaSueldos}">
            <DataGrid.Resources>
                <!--  Alineación derecha y formato numérico  -->
                <Style x:Key="RightAlignNumericCell" TargetType="TextBlock">
                    <Setter Property="TextAlignment" Value="Right" />
                    <Setter Property="Padding" Value="4,0,4,0" />
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="40"
                    Binding="{Binding Id}"
                    Header="No" />

                <DataGridTextColumn
                    Width="80"
                    Binding="{Binding CarnetIdentidad}"
                    Header="CI" />
                <DataGridTextColumn
                    Width="220"
                    Binding="{Binding ApellidosNombres}"
                    Header="Apellidos y Nombres" />
                <DataGridTextColumn
                    Width="80"
                    Binding="{Binding Nacionalidad}"
                    Header="Nac." />
                <DataGridTextColumn
                    Width="110"
                    Binding="{Binding FechaNacimiento, StringFormat=d}"
                    Header="F. Nac." />
                <DataGridTextColumn
                    Width="60"
                    Binding="{Binding Sexo}"
                    Header="Sexo" />
                <DataGridTextColumn
                    Width="160"
                    Binding="{Binding Ocupacion}"
                    Header="Ocupación" />
                <DataGridTextColumn
                    Width="110"
                    Binding="{Binding FechaIngreso, StringFormat=d}"
                    Header="F. Ingreso" />
                <DataGridTextColumn
                    Width="80"
                    Binding="{Binding DiasPagados}"
                    Header="Días pagados" />

                <!--  OTROS PAGOS  -->
                <DataGridTextColumn
                    Width="110"
                    Binding="{Binding HaberBasico, StringFormat=N2}"
                    ElementStyle="{StaticResource RightAlignNumericCell}"
                    Header="Haber Básico" />

                <DataGridTextColumn
                    Width="110"
                    Binding="{Binding BonoAntiguedad, StringFormat=N2}"
                    ElementStyle="{StaticResource RightAlignNumericCell}"
                    Header="Bono Antig." />

                <DataGridTextColumn
                    Width="110"
                    Binding="{Binding BonoProduccion, StringFormat=N2}"
                    ElementStyle="{StaticResource RightAlignNumericCell}"
                    Header="Bono Prod." />

                <DataGridTextColumn
                    Width="130"
                    Binding="{Binding AporteCoop334, StringFormat=N2}"
                    ElementStyle="{StaticResource RightAlignNumericCell}"
                    Header="Aporte Coop 3.34%" />

                <DataGridTextColumn
                    Width="120"
                    Binding="{Binding TotalGanado, StringFormat=N2}"
                    ElementStyle="{StaticResource RightAlignNumericCell}"
                    Header="Total Ganado" />

                <!--  DESCUENTOS  -->
                <DataGridTextColumn
                    Width="120"
                    Binding="{Binding Gestora1221, StringFormat=N2}"
                    ElementStyle="{StaticResource RightAlignNumericCell}"
                    Header="Gestora 12.21%" />

                <DataGridTemplateColumn
                    Width="120"
                    Header="RC-IVA 13%"
                    IsReadOnly="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock HorizontalAlignment="Right" Text="{Binding RcIva13, StringFormat=N2}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <TextBox HorizontalAlignment="Stretch" Text="{Binding RcIva13, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn
                    Width="120"
                    Binding="{Binding AporteSolidario05, StringFormat=N2}"
                    ElementStyle="{StaticResource RightAlignNumericCell}"
                    Header="Ap. Solid. 0.5%" />

                <DataGridTextColumn
                    Width="120"
                    Binding="{Binding OtrosDesc668, StringFormat=N2}"
                    ElementStyle="{StaticResource RightAlignNumericCell}"
                    Header="Otros 6.68%" />

                <DataGridTemplateColumn
                    Width="120"
                    Header="Otros Desc."
                    IsReadOnly="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock HorizontalAlignment="Right" Text="{Binding OtrosDescuentos, StringFormat=N2}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <TextBox HorizontalAlignment="Stretch" Text="{Binding OtrosDescuentos, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>


                <DataGridTextColumn
                    Width="120"
                    Binding="{Binding TotalDescuentos, StringFormat=N2}"
                    ElementStyle="{StaticResource RightAlignNumericCell}"
                    Header="Total Desc." />

                <DataGridTextColumn
                    Width="130"
                    Binding="{Binding LiquidoPagable, StringFormat=N2}"
                    ElementStyle="{StaticResource RightAlignNumericCell}"
                    Header="Líquido Pagable" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
